{
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.12.1",
  "scripts": {
    "dev": "pnpm --filter=./frontend dev",
    "deploy:gh": "pnpm --filter=./frontend build && git subtree split --prefix frontend/dist -b gh-pages && git push -f origin gh-pages:gh-pages && git branch -D gh-pages",
    "postinstall": "npx simple-git-hooks"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.4.5",
    "eslint": "^8.55.0",
    "eslint-define-config": "^2.0.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.1",
    "prettier-plugin-solidity": "^1.2.0",
    "simple-git-hooks": "^2.9.0"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm lint-staged"
  },
  "lint-staged": {
    "*": "eslint --fix",
    "*.sol": "prettier --write --plugin=prettier-plugin-solidity 'backend/contracts/**/*.sol'"
  }
}
