export interface Job {
  title: string
  tags: string[]
  description: string
  price: number
}

export interface Profile {
  title: string
  badges: string[]
  description: string
  budget: number
  star: number
  ratingCount: number
}
